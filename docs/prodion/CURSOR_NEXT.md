# Cursor next (app kernel, not hypervisor)

Repo expected: `eemmenisza/build-quantify`  
Branch expected: `claude/steel-takeoff-suite-qaas-parity`

If that repo is missing from GitHub.com under this account, create it or grant the Grok GitHub app access.

Priority slice (do not swap for OpenStack):
1. Drawing register
2. Extraction
3. Steel take-off
4. Revision comparison
5. Tender / BoQ

Locks:
- InferenceProvider abstraction
- HTTP 400 DRAWING_EMPTY
- Stable revision ids
- Runtime until cutover = volume D:
- Icom = migration target only
