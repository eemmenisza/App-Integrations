# data/crm

- `manifest.json` — safe pull metadata (committed)
- `snapshots/` — Monday/QaaS payloads with PII (**gitignored**; CI uploads as Actions artifacts)

See `docs/context/qaas-crm-people-sync.md`.
